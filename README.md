# Image-Labeling-Pipeline
“A Python-based pipeline that automatically generates images from text prompts and creates labeled datasets (CSV format) for machine learning and OCR model training. Useful for rapid dataset creation and experimentation.
